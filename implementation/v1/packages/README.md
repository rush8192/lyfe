# Shared artifact packages

Generated or distributable artifacts shared across client stacks belong here. Expected examples include generated TypeScript Protocol Buffer bindings, presentation metadata packages, and schema bundles.

Authoritative simulation domain models do not move into this directory merely to make them convenient for one client. Shared packages are produced from language-neutral contracts such as `protocol/` and validated rule presentation metadata.
